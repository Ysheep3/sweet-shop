<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sweet.cart.mapper.ShoppingCartMapper">
    <insert id="insertBatch">
        INSERT INTO shopping_cart
        (
        name,
        image,
        user_id,
        dish_id,
        setmeal_id,
        number,
        amount,
        )
        VALUES
        <foreach collection="shoppingCarts" item="item" separator=",">
            (
            #{item.name},
            #{item.image},
            #{item.userId},
            #{item.dishId},
            #{item.setmealId},
            #{item.number},
            #{item.amount},
            )
        </foreach>
    </insert>

    <select id="list" resultType="com.sweet.cart.entity.pojo.ShoppingCart" parameterType="com.sweet.cart.entity.pojo.ShoppingCart">
        select *
        from shopping_cart
        <where>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
        order by create_time desc
    </select>
</mapper>
